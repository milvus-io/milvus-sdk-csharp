# V2 SDK 文档（`Milvus.Client.V2`）

第二代 SDK（`Milvus.Client.V2`）的 API reference 等文档统一放在此目录。

约定：

- `docs/v1/` — 第一代 SDK（`Milvus.Client`）的原有文档（api reference / user guide / notebooks 等）。
- `docs/design/` — 设计文档，V2 设计说明见 `docs/design/v2_design.md`。
- `docs/v2/` — 本目录，后续 V2 的 reference 文档放这里。
